# Serverless API dependencies only — the deployed function reads Supabase and
# renders evidence packs; model training and ETL run in CI from
# requirements-pipeline.txt, keeping this bundle under Vercel's size limit.
fastapi>=0.110
psycopg2-binary>=2.9
pandas>=2.0
numpy>=1.26
matplotlib>=3.8
anthropic>=0.40
pytz>=2024.1
